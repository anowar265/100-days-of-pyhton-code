# Command to push code from replit to github

## git push --set -upstream origin main
### this command must be written from Shell 
### later we just need to commit all changes and Sync-with-remote or Pull or Push 